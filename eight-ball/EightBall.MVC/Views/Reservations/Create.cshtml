@model EightBall.Shared.Dtos.ReservationDto

@{
    ViewData["Title"] = "Rezervacija";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div>
    <h4>Podaci o rezervaciji</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Table.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Table.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Appointment.Start)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Appointment.Start)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Appointment.End)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Appointment.End)
        </dd>
    </dl>

    <form asp-action="Create">
        <input type="hidden" asp-for="@Model.TableId" />
        <input type="hidden" asp-for="@Model.AppointmentId" />
        <input type="submit" value="Potvrdi" class="btn btn-primary" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>